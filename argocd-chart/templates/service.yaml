apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-chart.fullname" . }}
  labels:
    {{- include "argocd-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-chart.selectorLabels" . | nindent 4 }}
